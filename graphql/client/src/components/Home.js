import React, {Component} from 'react';

import BookList from './BookList';

class Home extends Component {
    render() {
        return (
            <BookList/>
        );
    }
}

export default Home;
